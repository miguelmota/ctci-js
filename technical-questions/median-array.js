// unfinished

var array = [2,3,4,1];

function median(a) {
  var min = minheap.pop();
  var max = maxheap.pop();
  while (min < max) {
    min = minheap.pop():
    max = maxheap.pop();
  }
  return (min + max) / 2;
}

console.log(median(array));
